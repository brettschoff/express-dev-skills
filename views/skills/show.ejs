<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../stylesheets/style.css" type="text/css">
    <title>Skill: <%= skill.skill %></title>
</head>
<body>
    <h1>Skill: <%= skill.skill %></h1>
    <div>
        <h3>Am I proficient: <%= skill.isProficient ? 'Yes' : 'Not Yet' %></h3>
        <form action="/skills/<%= skill.id %>?_method=DELETE" method="POST">
            <button type="submit">Delete</button>
        </form>
        <form action="/skills/<%= skill.id %>/edit" method="GET">
        <button type="submit">Edit Skill</button>
        </form>
    </div>
</body>
</html>